<Window x:Class="PROJEKT_WPF_AK_RD.Views.getquestionmockupview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PROJEKT_WPF_AK_RD.Views"
        mc:Ignorable="d"
        Title="getquestionmockupview" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Liczba pytań:" VerticalAlignment="Center"/>
                <TextBox x:Name="AmountBox" Width="50" Margin="10,0"/>
                
                <TextBlock Text="Kategoria:" VerticalAlignment="Center" Margin="10,0"/>
                <ComboBox x:Name="CategoryBox" Width="150" DisplayMemberPath="Name" SelectedValuePath="Id" />
                
                <TextBlock Text="Trudność:" VerticalAlignment="Center" Margin="10,0"/>
                <ComboBox x:Name="DifficultyBox" Width="100"/>

                <TextBlock Text="Typ:" VerticalAlignment="Center" Margin="10,0"/>
                <ComboBox x:Name="TypeBox" Width="100"/>

                <Button Content="Pobierz pytania" Margin="10,0" Click="FetchQuestions_Click"/>
            </StackPanel>
        </StackPanel>

        <ListBox x:Name="QuestionsList" Grid.Row="1" />
    </Grid>
</Window>
